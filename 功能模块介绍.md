# XSS 自动化挖掘工具功能模块与技术实现说明

这份文档用于说明系统的功能模块、主要技术栈、核心实现路径和代码位置，便于从整体流程到具体模块快速理解项目实现。

## 目录

| 章节 | 用途 |
| --- | --- |
| [0. 系统讲解](#sec-0) | 开场时快速说明系统本质 |
| [1. 功能模块](#sec-1) | 按关注点快速定位相关模块 |
| [2. 系统整体闭环](#sec-2) | 从创建任务到报告导出的主流程 |
| [3. 关键技术在代码中的体现](#sec-3) | Flask、Vue、ORM、Scrapy、SSE 等 |
| [4. 核心功能模块](#sec-4) | 用户、任务、爬虫、分析、验证、报告 |
| [5. 静态分析与动态验证](#sec-5) | Source、Sink、Payload、验证结果 |
| [6. AI 辅助分析](#sec-6) | AI 在系统中负责什么、不负责什么 |
| [7. 数据库与报告](#sec-7) | 表结构、结果融合、报告导出 |
| [8. 局限性与后续改进](#sec-8) | 当前不足和后续优化方向 |
| [9. 核心理解要点](#sec-9) | 主要技术和模块的简要记忆 |

<a id="sec-0"></a>
## 0. 一句话讲清系统

本系统是一个面向 XSS 漏洞的自动化挖掘与验证工具：先用爬虫采集目标站点页面，再通过静态分析发现可疑的 Source 到 Sink 数据流，然后用动态验证发送安全测试 Payload 判断风险是否真实触发，最后结合 AI 辅助解释和报告导出形成完整检测闭环。

如果用第一性原理解释，XSS 的本质是：

> 不可信输入进入了 HTML 或 JavaScript 执行上下文，并且没有被正确过滤、编码或隔离。

所以系统围绕三个问题展开：

| 问题 | 系统怎么解决 |
| --- | --- |
| 输入从哪里来？ | 识别 `location.search`、`location.hash`、表单、URL、Cookie 等 Source |
| 输入流向哪里？ | 识别 `innerHTML`、`document.write`、`eval`、事件属性等 Sink |
| 是否真的能触发？ | 通过 query、form、hash 三类向量发送 Payload 做动态验证 |

<a id="sec-1"></a>
## 1. 功能模块与技术索引

| 关注点 | 对应章节 | 核心说明 |
| --- | --- | --- |
| Flask 后端怎么体现？ | [3.1 Flask + REST API + Blueprint](#sec-3-1) | `create_app()` 创建应用，`api_bp` 定义接口，`register_blueprint()` 挂载到 `/api` |
| Application Factory 是什么？ | [3.2 Application Factory 模式](#sec-3-2) | 用 `create_app()` 统一创建和配置 Flask 应用 |
| 前端 Vue 3 怎么体现？ | [3.3 Vue 3 CDN + Composition API](#sec-3-3) | CDN 引入 Vue，在 `setup()` 中用 `ref` 和 `computed` 管理状态 |
| 实时日志怎么实现？ | [3.4 EventSource + SSE](#sec-3-4) | 前端 `EventSource` 连接后端 `text/event-stream` 接口 |
| SQLAlchemy ORM 是什么？ | [3.5 SQLAlchemy ORM](#sec-3-5) | Python 类对应数据库表，对象操作代替手写 SQL |
| 爬虫怎么限制同域？ | [3.6 Scrapy Spider + LinkExtractor](#sec-3-6) | `allowed_domains` 来自目标 URL 主机名，并传给 `LinkExtractor` |
| 爬虫是否使用 CrawlSpider？ | [3.6 Scrapy Spider + LinkExtractor](#sec-3-6) | 代码实际是 `Spider + LinkExtractor`，不是直接继承 `CrawlSpider` |
| AutoThrottle 有什么用？ | [3.7 AutoThrottle 自动限速](#sec-3-7) | 根据响应情况动态调整请求速度，降低目标站点压力 |
| 静态分析和动态验证区别？ | [5.1 二者区别](#sec-5-1) | 静态分析找可疑点，动态验证证明是否能触发 |
| Source 和 Sink 是什么？ | [5.2 Source 与 Sink](#sec-5-2) | Source 是用户可控输入，Sink 是危险输出或执行点 |
| Payload 是什么？ | [5.3 Payload](#sec-5-3) | 用来测试漏洞是否存在的输入载荷 |
| AI 会不会误判？ | [6.1 AI 的定位](#sec-6-1) | AI 只做解释、建议和验证计划，不单独作为最终判定依据 |
| 数据库表怎么设计？ | [7.1 核心数据表](#sec-7-1) | Job、Page、Finding、DynamicVerification、AIReport 等 |

<a id="sec-2"></a>
## 2. 系统整体闭环

### 2.1 主流程

```text
用户创建扫描任务
  ↓
Flask API 接收 URL、深度、页面数等参数
  ↓
JobRunner 创建任务并启动 Scrapy 子进程
  ↓
Scrapy 爬取同域页面并推送 page 事件
  ↓
静态分析器分析 HTML / JavaScript，生成 finding 事件
  ↓
Runner 将页面、风险点和日志写入数据库
  ↓
动态验证模块对 query / form / hash 注入 Payload
  ↓
AI 模块生成解释、误报提示和修复建议
  ↓
前端展示报告、日志、工作台和导出结果
```

### 2.2 主链路代码定位

| 阶段 | 代码位置 | 说明 |
| --- | --- | --- |
| 前端创建任务 | [web/index.html:406](web/index.html#L406) | 调用后端 `/api/jobs` |
| API 接收任务 | [server/api.py:169](server/api.py#L169) | `create_job()` |
| Runner 创建任务 | [server/runner.py:32](server/runner.py#L32) | 生成 `Job` 并入库 |
| 启动 worker | [server/runner.py:48](server/runner.py#L48) | 子进程执行爬虫 |
| 爬虫处理页面 | [server/spider.py:73](server/spider.py#L73) | `parse()` 处理响应 |
| 静态分析 | [server/analyzer.py:71](server/analyzer.py#L71) | `analyze_html()` |
| 动态验证 | [server/dynamic_verify.py:119](server/dynamic_verify.py#L119) | query / form / hash 验证 |
| 报告聚合 | [server/api.py:1201](server/api.py#L1201) | 聚合 Page、Finding、Verification |

<a id="sec-3"></a>
## 3. 关键技术在代码中的体现

<a id="sec-3-1"></a>
### 3.1 Flask + REST API + Blueprint

一句话：Flask 负责提供后端 HTTP 服务，REST API 负责前后端通信，Blueprint 负责把接口模块化挂载到 `/api` 下。

代码位置：

| 代码 | 位置 | 作用 |
| --- | --- | --- |
| `app = Flask(__name__)` | [server/__init__.py:35](server/__init__.py#L35) | 创建 Flask 应用 |
| `api_bp = Blueprint("api", __name__)` | [server/api.py:38](server/api.py#L38) | 创建 API 蓝图 |
| `app.register_blueprint(api_bp, url_prefix="/api")` | [server/__init__.py:61](server/__init__.py#L61) | 把所有 API 挂载到 `/api` |
| `@api_bp.post("/jobs")` | [server/api.py:169](server/api.py#L169) | 创建扫描任务接口 |
| `@api_bp.get("/jobs")` | [server/api.py:190](server/api.py#L190) | 查询任务列表接口 |

表述参考：

> 后端使用 Flask 构建轻量级 REST API。应用实例在 `create_app()` 中创建，具体接口集中在 `server/api.py`。这些接口不是直接写在 `app` 上，而是先定义在 `api_bp` 蓝图中，再通过 `register_blueprint()` 统一挂载到 `/api` 路径下。这样应用初始化和接口定义是分离的，结构更清晰。

追问回答：

> 当前系统主要使用一个 API Blueprint 统一管理后端接口，业务逻辑再拆分到 `auth.py`、`runner.py`、`analyzer.py`、`dynamic_verify.py` 等模块中。

<a id="sec-3-2"></a>
### 3.2 Application Factory 模式

一句话：Application Factory 就是用一个函数来创建 Flask 应用，而不是在全局直接写死一个 app。

代码位置：

| 代码 | 位置 | 作用 |
| --- | --- | --- |
| `def create_app() -> Flask:` | [server/__init__.py:19](server/__init__.py#L19) | 应用工厂函数 |
| `app = create_app()` | [run_dev.py:10](run_dev.py#L10) | 启动时创建 app |
| `app.run(...)` | [run_dev.py:11](run_dev.py#L11) | 启动后端服务 |

`create_app()` 里完成的事情：

| 步骤 | 代码位置 | 说明 |
| --- | --- | --- |
| 创建 Flask app | [server/__init__.py:35](server/__init__.py#L35) | `Flask(__name__)` |
| 配置数据库 | [server/__init__.py:42](server/__init__.py#L42) | `app.config.from_mapping(...)` |
| 配置跨域 | [server/__init__.py:55](server/__init__.py#L55) | `CORS(app, ...)` |
| 初始化 ORM | [server/__init__.py:57](server/__init__.py#L57) | `db.init_app(app)` |
| 创建数据表 | [server/__init__.py:59](server/__init__.py#L59) | `db.create_all()` |
| 注册 API 蓝图 | [server/__init__.py:61](server/__init__.py#L61) | `register_blueprint()` |

表述参考：

> Application Factory 模式在本系统中体现为 `create_app()` 函数。它负责创建 Flask 实例，并集中完成数据库配置、跨域配置、ORM 初始化和 Blueprint 注册。启动时由 `run_dev.py` 调用 `create_app()` 得到 app 对象，再通过 `app.run()` 启动服务。

<a id="sec-3-3"></a>
### 3.3 Vue 3 CDN + Composition API

一句话：前端没有使用复杂构建工具，而是通过 CDN 引入 Vue 3，用 Composition API 管理页面状态。

代码位置：

| 代码 | 位置 | 作用 |
| --- | --- | --- |
| Vue CDN | [web/index.html:9](web/index.html#L9) | 引入 Vue 3 浏览器版本 |
| `createApp` / `ref` / `computed` | [web/index.html:18](web/index.html#L18) | 使用 Composition API |
| `createApp({ setup() { ... } })` | [web/index.html:20](web/index.html#L20) | 创建单页应用 |
| `jobs = ref([])` | [web/index.html:35](web/index.html#L35) | 任务列表响应式状态 |
| `logs = ref([])` | [web/index.html:37](web/index.html#L37) | 实时日志响应式状态 |
| `selectedJob = computed(...)` | [web/index.html:124](web/index.html#L124) | 当前任务计算属性 |

表述参考：

> 前端通过 CDN 直接引入 Vue 3，在 `setup()` 中使用 Composition API。`ref()` 用来保存任务列表、日志、报告、登录状态等响应式数据，`computed()` 用来根据已有状态计算当前选中任务等派生数据。数据变化后，Vue 会自动更新界面，不需要手动操作 DOM。

<a id="sec-3-4"></a>
### 3.4 EventSource + SSE

一句话：EventSource 是前端接收服务器推送事件的浏览器 API，SSE 是后端持续推送日志的协议形式。

代码位置：

| 代码 | 位置 | 作用 |
| --- | --- | --- |
| `new EventSource(eventsUrl)` | [web/index.html:376](web/index.html#L376) | 前端建立 SSE 连接 |
| `addEventListener("log", ...)` | [web/index.html:377](web/index.html#L377) | 接收日志事件 |
| `logs.value.push(newLog)` | [web/index.html:381](web/index.html#L381) | 追加日志并自动刷新页面 |
| `@api_bp.get("/jobs/<job_id>/events")` | [server/api.py:363](server/api.py#L363) | 后端 SSE 接口 |
| `Content-Type: text/event-stream` | [server/api.py:367](server/api.py#L367) | SSE 响应类型 |
| `yield f"event: {evt.type}\n"` | [server/events.py:38](server/events.py#L38) | 事件流格式 |

表述参考：

> 扫描任务运行时间较长，如果前端一直轮询会比较低效。因此系统使用 SSE 实现实时日志推送。前端通过 `EventSource` 连接 `/api/jobs/<job_id>/events`，后端返回 `text/event-stream` 流式响应。爬虫和任务执行层产生的日志、页面、风险发现事件会实时推送到前端。

<a id="sec-3-5"></a>
### 3.5 SQLAlchemy ORM

一句话：SQLAlchemy ORM 把数据库表映射成 Python 类，让后端通过对象操作数据库。

代码位置：

| 代码 | 位置 | 作用 |
| --- | --- | --- |
| `db = SQLAlchemy()` | [server/db.py:3](server/db.py#L3) | 创建 ORM 对象 |
| `db.init_app(app)` | [server/__init__.py:57](server/__init__.py#L57) | 绑定 Flask 应用 |
| `db.create_all()` | [server/__init__.py:59](server/__init__.py#L59) | 根据模型创建表 |
| `class Job(db.Model)` | [server/models.py:37](server/models.py#L37) | 任务表模型 |
| `class Page(db.Model)` | [server/models.py:57](server/models.py#L57) | 页面表模型 |
| `class Finding(db.Model)` | [server/models.py:73](server/models.py#L73) | 风险发现表模型 |
| `class DynamicVerification(db.Model)` | [server/models.py:120](server/models.py#L120) | 动态验证表模型 |

表述参考：

> ORM 的本质是对象关系映射。在本系统中，`Job` 类对应扫描任务表，`Page` 类对应页面表，`Finding` 类对应漏洞发现表，`DynamicVerification` 类对应动态验证表。系统通过 `db.session.add()` 保存对象，通过 `db.session.commit()` 提交事务，通过 `Job.query` 或 `db.session.get()` 查询数据。这样可以减少手写 SQL，提高代码可维护性。

<a id="sec-3-6"></a>
### 3.6 Scrapy Spider + LinkExtractor

一句话：爬虫负责从目标 URL 出发，提取同域链接并递归爬取页面。

注意：PPT 中如果写了 `CrawlSpider + LinkExtractor`，代码实现上更准确的是 `Spider + LinkExtractor`。代码没有直接继承 `CrawlSpider`。

代码位置：

| 代码 | 位置 | 作用 |
| --- | --- | --- |
| `class DeepSpider(Spider)` | [server/spider.py:22](server/spider.py#L22) | 自定义 Scrapy 爬虫 |
| `from scrapy.linkextractors import LinkExtractor` | [server/spider.py:16](server/spider.py#L16) | 引入链接提取器 |
| `parsed = urlparse(target_url)` | [server/spider.py:58](server/spider.py#L58) | 解析目标 URL |
| `self.allowed_domains = [parsed.hostname]` | [server/spider.py:60](server/spider.py#L60) | 设置允许域名 |
| `LinkExtractor(allow_domains=self.allowed_domains)` | [server/spider.py:64](server/spider.py#L64) | 限制只提取同域链接 |
| `extract_links(response)` | [server/spider.py:132](server/spider.py#L132) | 从页面提取链接 |
| `yield scrapy.Request(...)` | [server/spider.py:137](server/spider.py#L137) | 继续请求下一层页面 |

表述参考：

> 爬虫模块使用 Scrapy 实现。系统从用户输入的目标 URL 中解析主机名，将其设置为 `allowed_domains`，再传给 `LinkExtractor`。这样链接提取器只会保留目标域名下的链接，避免爬虫跳转到第三方网站。代码中使用基础 `Spider` 手动控制深度和页面数量，逻辑更直接，也方便在每个页面处理时插入静态分析和事件推送。

<a id="sec-3-7"></a>
### 3.7 AutoThrottle 自动限速

一句话：AutoThrottle 会根据目标站点响应情况自动调整请求间隔，降低扫描压力。

代码位置：

| 代码 | 位置 | 作用 |
| --- | --- | --- |
| `AUTOTHROTTLE_ENABLED: True` | [server/spider.py:32](server/spider.py#L32) | 启用自动限速 |
| `AUTOTHROTTLE_START_DELAY: 0.25` | [server/spider.py:33](server/spider.py#L33) | 初始延迟 |
| `AUTOTHROTTLE_MAX_DELAY: 5.0` | [server/spider.py:34](server/spider.py#L34) | 最大延迟 |
| `CONCURRENT_REQUESTS: 16` | [server/spider.py:35](server/spider.py#L35) | 总并发限制 |
| `CONCURRENT_REQUESTS_PER_DOMAIN: 8` | [server/spider.py:36](server/spider.py#L36) | 单域名并发限制 |

表述参考：

> 系统启用了 Scrapy 的 AutoThrottle 自动限速机制。它会根据目标站点响应速度动态调整请求间隔，避免请求过快。同时系统还设置了总并发和单域名并发限制，使爬取过程更可控。

### 3.8 Selenium + httpx

一句话：`httpx` 用于轻量 HTTP 请求验证，`Selenium` 用于真实浏览器环境下验证 DOM 型 XSS。

表述参考：

> 动态验证分为两类场景。对于 query 参数和表单提交，系统可以用 `httpx` 发送 HTTP 请求并检查响应内容；对于 DOM 型 XSS，仅看 HTTP 响应不够，因为漏洞可能要等浏览器执行 JavaScript 后才出现，所以系统引入 Selenium 无头浏览器加载页面并观察浏览器侧行为。

<a id="sec-4"></a>
## 4. 核心功能模块

| 模块 | 主要职责 | 关键代码 |
| --- | --- | --- |
| 用户认证 | 注册、登录、Bearer Token 校验 | [server/api.py:78 register](server/api.py#L78)、[server/api.py:110 login](server/api.py#L110)、[server/auth.py:22 issue_token](server/auth.py#L22) |
| 任务管理 | 创建任务、启动任务、状态流转 | [server/api.py:169 create_job](server/api.py#L169)、[server/runner.py:23 JobRunner](server/runner.py#L23)、[server/runner.py:48 start_job](server/runner.py#L48) |
| 页面爬取 | 同域页面采集、HTML 保存、日志推送 | [server/spider.py:22 DeepSpider](server/spider.py#L22)、[server/spider.py:73 parse](server/spider.py#L73)、[server/spider.py:132 extract_links](server/spider.py#L132) |
| 静态分析 | Source、Sink、内联事件、伪协议、轻量数据流 | [server/analyzer.py:71 analyze_html](server/analyzer.py#L71)、[server/js_ast_analyzer.py:114 analyze_script_flows](server/js_ast_analyzer.py#L114) |
| 动态验证 | query、form、hash 注入复测 | [server/dynamic_verify.py:119 run_dynamic_verification](server/dynamic_verify.py#L119)、[server/dynamic_verify.py:283 retest_page](server/dynamic_verify.py#L283)、[server/dynamic_verify.py:467 suggest_payloads_for_page](server/dynamic_verify.py#L467) |
| AI 辅助 | 解释、修复建议、Payload 生成、多轮验证计划 | [AIModelAPI](ai/api.py)、[ai/api.py:59 recommend_validation_plan](ai/api.py#L59)、[ai/api.py:81 generate_payloads](ai/api.py#L81) |
| 报告导出 | 聚合页面、风险、验证、AI 结果并导出 | [server/report_export.py:14 render_report_json](server/report_export.py#L14)、[server/report_export.py:18 render_report_html](server/report_export.py#L18)、[server/api.py:336 export](server/api.py#L336) |

<a id="sec-5"></a>
## 5. 静态分析与动态验证

<a id="sec-5-1"></a>
### 5.1 静态分析和动态验证的区别

| 对比项 | 静态分析 Static Analysis | 动态验证 Dynamic Verification |
| --- | --- | --- |
| 是否运行页面 | 不运行 | 运行或模拟访问 |
| 主要依据 | HTML / JavaScript 源码 | HTTP 响应、浏览器行为 |
| 优点 | 快、覆盖面广 | 准确性更强 |
| 缺点 | 可能误报 | 依赖 Payload、页面可达性 |
| 系统作用 | 找可疑点 | 证明风险是否能触发 |

表述参考：

> 静态分析像代码审计，负责快速发现可疑代码；动态验证像实验验证，负责证明这个风险是否真的能触发。两者结合可以提高效率，也能减少误报。

<a id="sec-5-2"></a>
### 5.2 Source 与 Sink

`Source` 是用户可控输入，也就是攻击者可能控制的数据来源。

常见 Source：

```js
location.search
location.hash
document.URL
document.cookie
```

`Sink` 是危险输出点或危险执行点。如果不可信数据进入 Sink，就可能触发 XSS。

常见 Sink：

```js
innerHTML
outerHTML
document.write()
eval()
new Function()
```

示例：

```js
const name = location.search;
document.body.innerHTML = name;
```

这里 `location.search` 是 Source，`innerHTML` 是 Sink。如果中间没有过滤或编码，就存在 XSS 风险。

<a id="sec-5-3"></a>
### 5.3 Payload

Payload 是测试载荷，也就是用来验证漏洞是否存在的输入内容。

在系统中，Payload 不是为了攻击真实站点，而是用于安全验证。例如通过特殊标记观察是否回显、是否进入危险上下文、是否被浏览器执行。

表述参考：

> Payload 可以理解为测试输入。系统把 Payload 注入不同输入点，通过响应内容和浏览器行为判断漏洞是否真实存在。

### 5.4 三类动态验证向量

| 向量 | 英文 | 说明 |
| --- | --- | --- |
| Query 参数注入 | Query Parameter Injection | 把 Payload 放到 URL 参数中 |
| 表单提交注入 | Form Submission Injection | 自动解析表单并提交 Payload |
| Hash 片段注入 | Hash Fragment Injection | 把 Payload 放到 `#` 后面，验证 DOM 型 XSS |

示例：

```text
https://example.com/search?q=PAYLOAD
https://example.com/page#PAYLOAD
```

### 5.5 伪协议

伪协议是看起来像 URL 协议，但不是普通网络访问协议，而是触发浏览器特殊行为的形式。

高风险例子：

```html
<a href="javascript:alert(1)">click</a>
<a href="data:text/html,<script>alert(1)</script>">open</a>
```

`javascript:` 会让浏览器执行后面的 JavaScript；`data:text/html` 可能让浏览器把 URL 中的数据当成 HTML 解析。如果这些内容来自用户输入，就可能形成 XSS。

<a id="sec-6"></a>
## 6. AI 辅助分析

<a id="sec-6-1"></a>
### 6.1 AI 的定位

AI 在系统中是辅助模块，不是最终判定模块。

| AI 做什么 | AI 不做什么 |
| --- | --- |
| 解释漏洞原因 | 不单独决定漏洞一定存在 |
| 提示误报可能 | 不替代动态验证 |
| 生成修复建议 | 不替代人工复核 |
| 生成 Payload 候选 | 不直接攻击目标 |
| 制定多轮验证计划 | 不绕过系统安全边界 |

表述参考：

> AI 主要负责提高结果可读性。系统把静态发现项、动态验证摘要和页面上下文提交给 AI，让它生成解释、误报提示、修复建议和验证计划。但最终判断仍然基于静态规则和动态验证证据，AI 不单独作为漏洞判定依据。

### 6.2 AI 接口

| 模块 | 代码位置 | 作用 |
| --- | --- | --- |
| AI HTTP 调用 | [AIModelAPI](ai/api.py) | 兼容 OpenAI 风格接口 |
| 业务封装 | [AIAnalyzer](ai/analyzer.py) | 统一返回结构化结果 |
| API 入口 | [server/api.py:374 任务级 AI 分析](server/api.py#L374)、[server/api.py:975 页面 AI 解释](server/api.py#L975)、[AI Payload 生成](server/api.py) | 提供 AI 分析、AI 解释、AI Payload 接口 |

<a id="sec-7"></a>
## 7. 数据库与报告

<a id="sec-7-1"></a>
### 7.1 核心数据表

| 模型 | 表名 | 保存内容 |
| --- | --- | --- |
| `User` | `users` | 用户名、密码哈希、Token |
| `Job` | `jobs` | 扫描任务、目标 URL、深度、页数、状态 |
| `Page` | `pages` | 页面 URL、状态码、HTML、sha256 |
| `Finding` | `findings` | 静态分析发现项 |
| `Log` | `logs` | 任务运行日志 |
| `DynamicVerification` | `dynamic_verifications` | 动态验证向量、Payload、状态、证据 |
| `AIReport` | `ai_reports` | AI 分析报告 |
| `AIPayloadReport` | `ai_payload_reports` | AI 生成 Payload 历史 |
| `FindingStatus` | `finding_statuses` | 人工复核状态 |

### 7.2 报告怎么形成

报告不是单独生成的孤立文件，而是从数据库中聚合多类结果：

```text
Job
  + Page
  + Finding
  + DynamicVerification
  + AIReport
  + FindingStatus
  ↓
报告页面 / HTML 导出 / JSON 导出
```

表述参考：

> 报告生成阶段会聚合任务信息、爬取页面、静态风险发现、动态验证结果和 AI 报告。这样报告既能看到代码层面的可疑点，也能看到运行时验证证据。

<a id="sec-8"></a>
## 8. 局限性与后续改进

| 当前局限 | 原因 | 后续改进 |
| --- | --- | --- |
| 登录态页面覆盖不足 | Cookie、验证码、复杂登录流程较难自动处理 | 增加登录态维护和 Cookie 导入 |
| 前端路由覆盖不足 | SPA 路由懒加载页面不一定能被普通爬虫发现 | 增加浏览器驱动的路由探索 |
| 静态分析有漏报 | 正则和轻量 AST 难覆盖复杂 JS 数据流 | 引入 Babel AST 和更完整污点传播 |
| Payload 覆盖有限 | 高级绕过场景很多 | 扩展编码、上下文相关 Payload |
| AI 稳定性受外部接口影响 | 依赖网络、模型和配额 | 增加缓存、降级策略和本地规则兜底 |

表述参考：

> 当前系统重点完成轻量级闭环，不追求覆盖所有复杂 Web 场景。后续可以从登录态维护、前端路由探索、AST 污点传播和存储型 XSS 跨页面追踪几个方向继续增强。

<a id="sec-9"></a>
## 9. 核心理解要点

| 主题 | 一句话 |
| --- | --- |
| 系统本质 | 自动寻找不可信输入到危险输出点的路径，并验证是否能触发 |
| Flask | 提供 REST API，Blueprint 统一挂载到 `/api` |
| Application Factory | `create_app()` 统一创建和配置 Flask 应用 |
| Vue 3 | CDN 引入，`ref` 和 `computed` 实现响应式页面 |
| SSE | `EventSource` 接收后端实时日志 |
| ORM | Python 类映射数据库表 |
| 爬虫 | `Spider + LinkExtractor` 提取同域链接 |
| allowed_domains | 限制只扫描目标域名 |
| AutoThrottle | 自动调整请求速度 |
| 静态分析 | 不运行代码，找 Source 到 Sink 的可疑路径 |
| 动态验证 | 发送 Payload，证明风险是否真实触发 |
| AI | 辅助解释，不单独做最终漏洞判定 |




